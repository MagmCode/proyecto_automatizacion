import { CedulaLengthDirective } from './cedula-length.directive';

describe('CedulaLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CedulaLengthDirective();
    expect(directive).toBeTruthy();
  });
});
