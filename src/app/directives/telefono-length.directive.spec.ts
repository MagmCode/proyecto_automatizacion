import { TelefonoLengthDirective } from './telefono-length.directive';

describe('TelefonoLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefonoLengthDirective();
    expect(directive).toBeTruthy();
  });
});
